# @readable-ui/react

React bindings for readable-ui.

Use `defineDualComponent` to declare a component with both an HTML render and a Markdown serializer. Both faces live in one place.

See the [repo README](../../README.md) for the overall project.
