# @readable-ui/mcp

Exposes readable-ui actions as Model Context Protocol tools.

Helpers for parsing and building action URIs (e.g. `mcp://tool/createUser?id=123`) that appear inside Markdown-rendered pages so agents can trigger operations without browser automation.

See the [repo README](../../README.md) for the overall project.
