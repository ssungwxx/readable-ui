# @readable-ui/core

Framework-agnostic serializer and component registry for readable-ui.

Provides the contract every renderer relies on: how a dual component declares its Markdown counterpart, and how a tree of such components is serialized through mdast into GFM Markdown.

See the [repo README](../../README.md) for the overall project.
