# @readable-ui/next

Next.js adapter for readable-ui.

Provides content negotiation helpers so a single route can return HTML for humans and Markdown for agents based on the `Accept` header.

See the [repo README](../../README.md) for the overall project.
